import 'package:flutter/material.dart';

import 'UI/select_screen.dart';
import 'UI/unit_conversion.dart';

void main() {
  runApp( const MaterialApp(
    debugShowCheckedModeBanner: false,
    home: SelectScreen(),
  ));
}
